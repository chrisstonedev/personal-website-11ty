{% extends "base.njk" %}

{% block main %}
    {{ content | safe }}
    <ul>
        {%- for post in collections.projects | sort(false, false, "data.title") -%}
            <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
        {%- endfor -%}
    </ul>
{% endblock %}